{{#partial "content"}}
       <div class="row row-cols-1 row-cols-md-2">
           {{#each squad}}
               <div class="col">
                   <div class="card h-100">
                       <p class="fw-bolder">{{name}}</p>
                       <p>{{size}}</p>
                       <p>{{cause}}</p>
                   </div>
               </div>
           {{/each}}

       </div>
{{/partial}}
{{> layout.hbs}}